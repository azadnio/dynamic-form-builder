name: Deploy Angular App (dynamic-form-builder) to GitHub Pages

on:
  push:
    branches:
      - main  # or 'master' depending on your repo

permissions:
  contents: write  # to allow deployment to gh-pages branch

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build -- --configuration production

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages         # target branch for deployment
          folder: dist/dynamic-form-builder/browser      # path to build output folder (new version of angular)